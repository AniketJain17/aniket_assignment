{
  "name": "@aniket/root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna exec --scope @aniket/app -- yarn start",
    "build": "lerna exec --scope @aniket/app -- yarn build"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  }
}